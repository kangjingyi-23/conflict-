# 慢慢说 · Design Spec V2

更新时间：2026-09-10  
文档状态：当前 HTML 的设计基线与后续完善要求  
基准：[relationship-mini-program-prototype.html](relationship-mini-program-prototype.html)，2026-09-10 08:17 更新  
配套：[PRD](PRD.md)、[Tech Spec](TECH_SPEC.md)；[Brand Spec](brand-spec.md) 为辅助参考，存在差异时以本文件所列实际 CSS 为准  
核对方式：HTML 结构、样式及交互脚本；本次未完成最新版浏览器视觉验收，已有旧截图不作为当前效果依据

## 1. 品牌与体验目标

暂用品牌“慢慢说”，以小动物和自然对话降低表达压力。核心功能名固定为“探索自己”“冲突复盘”。标题可以温柔，操作按钮仍须直接说明动作。

主体验是：先听自己的感受，分别看清事实与解释，再选择一个可做到的下一步行动。涉及对方时说明信息来源和参与意愿；涉及安全、隐私、绑定、身体记录和删除时，文案清晰直接。

不使用审判庭、法槌、天平、胜败印章、责任百分比或关系评分视觉。可爱来自四种小动物与轻巧措辞，不堆爱心，不用“宝宝”等称呼，不将身体状况解释为争执原因。

## 2. 信息架构和导航

### 2.1 产品内导航

首页呈现两张纵向主入口卡：“探索自己”“冲突复盘”，下方是一张说明探索与复盘关系、提示本地模板演示的卡片。首页没有最近记录列表、关怀卡、历史 Tab 或独立头像入口。

底部任务栏按“探索自己 / 我 / 冲突复盘”排列。中央“我”用所选动物头像和文字，进入“我的小角落”。探索结果和复盘页属于对应任务；`mine` 与 `companion` 共用“我”的选中态。

“我的”依次承载性别与头像、伴侣与关怀、关系画像、历史摘要、授权管理、导出/删除/隐私说明。经期关怀不升级为首页核心入口。

欢迎页隐藏任务栏；受邀者演示隐藏任务栏和返回按钮，并限制普通页面切换。其余二级页按应用内页面栈显示“返回”。首页与欢迎页不显示返回。

### 2.2 原型外壳与响应式

| 区域 / 断点 | 实际 CSS |
|---|---|
| 桌面外壳 `.prototype-shell` | 最大宽 1180px；三列 `minmax(280px,360px) minmax(390px,430px) minmax(260px,340px)`；间隙 24px |
| 左侧说明 | 8 个流程切换按钮，用于评审跳转，不属于正式产品导航 |
| 中间手机 | 最大宽 430px；比例 390:844；外框圆角 36px、内屏 28px；桌面 sticky top 24px |
| 右侧说明 | V2 信息架构和可用性路径，属于演示辅助 |
| ≤1040px | 外壳变为单列，手机居中且取消 sticky，说明栏仍显示 |
| ≤520px | 隐藏左右说明栏，移除手机边框、阴影与外壳圆角，宽 100vw、最小高 100vh |

这是 HTML 原型的布局规则，不能直接当成微信原生页面容器。移动端滚动、虚拟键盘和底部安全区仍需实机验证。

## 3. 色彩、字体与基础规格

### 3.1 色彩 Token

视觉已由旧版粉色转为暖米白底、陶土色强调、棕色文字。以下值来自 HTML `:root`，替代旧版 `#E96A9A` 主色方案。

| Token | 实际值 | 用途 |
|---|---|---|
| `--bg` | `oklch(96.8% 0.024 70)` | 页面底色 |
| `--surface` | `oklch(98.7% 0.012 78)` | 卡片、输入、任务栏 |
| `--fg` | `oklch(30% 0.035 48)` | 主文字 |
| `--muted` | `oklch(50% 0.038 52)` | 说明文字 |
| `--border` | `oklch(89.5% 0.033 65)` | 边框和分隔 |
| `--accent` | `oklch(55% 0.12 43)` | 主按钮、选中态、小动物耳朵 |
| `--accent-on` | `oklch(100% 0 0)` | 主按钮文字 |
| `--accent-soft` | `color-mix(in oklch, var(--accent) 12%, var(--surface))` | 选项和任务栏激活底色 |
| `--accent-fog` | `color-mix(in oklch, var(--accent) 7%, var(--bg))` | 探索入口和轻强调 |
| `--success` | `oklch(53.45% 0.0854 170.61)` | 预留成功语义 Token，当前完成状态主要用文字 |
| `--warn` | `oklch(62.64% 0.1248 70.45)` | 复盘卡辅助色 |
| `--danger` | `oklch(50.03% 0.1821 29.51)` | 风险、错误、删除 |
| `--danger-soft` | `color-mix(in oklch, var(--danger) 10%, var(--surface))` | 危险提示底色 |

复盘入口使用 surface 与 warn 8% 混色。安全页使用 surface 与 border 30% 混色，提示卡保留低饱和红色。页面外部有轻微径向/纵向渐变，不使用强紫蓝或荧光粉。

### 3.2 字体

主标题和正文共用系统字体栈：`-apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", system-ui, sans-serif`。元信息使用 `ui-monospace, "JetBrains Mono", "SF Mono", Menlo, monospace`，不加载外部字体文件。

| 文本类型 | 当前大小 / 规则 |
|---|---|
| 桌面说明主标题 | 28px |
| 手机页主标题 `.mini-title` | 24px，760 字重 |
| 卡片标题 | 20px，760 字重 |
| 正文、说明、主要按钮 | 14px；桌面说明段落为 15px |
| 字段标签、通知、选项 | 13px |
| 错误、步骤、报告分区、元信息 | 12px |
| 任务栏辅助字 | 11px；中央“我”为 12px |

正文行高 1.5，标题行高 1.22；避免用缩小文字解决长文堆叠。字号与触控标准是不同约束，正式发布前单独验收。

### 3.3 间距、圆角和固定层

| 元素 | 当前规格 |
|---|---|
| 页面内边距 | `14px 16px 104px`，底部为任务栏留白 |
| 有返回按钮的页面 | 顶部内边距 54px |
| 卡片 `.card/.entry-card` | 圆角 18px、内边距 16px、1px 边框 |
| 输入框、通知、个人选项 | 输入框与通知圆角 12px，小型个人选项为 10px；步骤标签为胶囊圆角 |
| 通用内容堆叠 | gap 12px；报告分区内 gap 10px |
| 双按钮组 | 等宽两列，gap 10px，顶部间距 14px |
| 主/次/危险按钮 | 最小高 44px，胶囊圆角 999px，内边距 10px 16px |
| 底部任务栏 | 左右 16px、底部 14px、内边距 8px、圆角 24px；列宽 `1fr 56px 1fr` |
| 任务键 | 常规最小高 54px；中央头像键宽 56px、高 64px、圆角 24px |
| 返回按钮 | 当前最小高 34px；尚未达到 44px 目标 |
| Toast / 删除遮罩 | z-index 30 / 40；任务栏 22，返回 24 |

`--radius-md` 虽为 12px，主要卡片实际使用 `--radius-lg:18px`；不要继续以“所有卡片 12px”描述最新版。阴影主要用于外壳、浮层和关键按钮。

## 4. 页面与组件规格

### 4.1 欢迎与个人选择 `welcome`

顶部“你好呀，这里是慢慢说 / 先选个小伙伴吧”，下方为个人选择器、产品说明、边界、安全提示、合并确认勾选和开始按钮。

个人选择器采用两个 `fieldset/legend`：性别四选一（含“暂不透露”），头像四选一（小狗、小猫、小兔、小熊）。头像四等分网格，选中用边框和浅强调底色，并更新 `aria-pressed`。默认暂不透露、小狗；修改同步到“我的”和任务栏头像。

动物由 CSS 绘制，共用脸、眼睛、鼻子，耳朵通过 `data-animal` 改变；无需额外图片资产。头像不表示性格、依恋类型或伴侣角色。

未勾选开始按钮禁用，实际 disabled 不透明度为 .48。该勾选目前合并成年人、隐私和 AI 使用说明，没有可阅读的完整协议页，应列为后续补充。

### 4.2 首页 `home`

主标题“今天，想从哪里说起？”，副文案邀请用户选择一项任务。两张入口卡最小高 174px，按钮分别“开始探索”“开始复盘”；有显式保存的草稿时，后者变为“继续草稿”。

探索卡偏静，复盘卡用轻微暖色区分。下方说明卡直接指出本地模板、未接真实 AI。不要加入关系分数、历史列表或经期提醒来稀释两个任务。

### 4.3 探索与当前线索 `self-quiz / self-result`

题页：页头、`1/6` 进度、8px 高进度条、单题卡、可换行选项、上一题/下一步。选择不自动换题；未选时就地出现错误。最后需要项允许 1–3 个，第四项提示先替换已有选择。

结果为五张纵向卡：需要、可能引发共鸣的时刻、自述冲突反应、一小步、回答来源。没有雷达图、维度分数、人格类型章或“保存私密档案”的独立按钮。来源数量为回答和选择计数；固定提示不是经过评分的推断。

结果中可进入复盘，页头可进入“我的”。部分答案缺失时展示占位提示；完整/未完成结果的明确视觉区分仍需补充。

### 4.4 六步复盘 `create-review`

顶部标题“我从这场冲突感受到的”，水平步骤条可横向滚动。六步为事件信息、我的视角、感受与需要、希望解决的问题、安全确认、AI 复盘。步骤条只表示进度，不作为任意跳转控件。

输入依次为标题/发生时间、事实/解释、感受/需要、希望理解/愿意调整/下一步、安全勾选。时间目前是文本输入；感受与需要为文本域，不是旧稿中的标签与强度滑块。

最后一步显示原生 `details/summary`：“补充对方的视角（选填）”，包含来源选择、三段文本和风险勾选。已有补充时默认展开。其下为模板提示、生成按钮、两个模拟失败按钮；模拟按钮属于原型测试功能。

底部保留上一步、下一步和“先存下来，歇一会儿”。未完成反馈目前是通用提示，正式版本应定位到具体缺失字段。

### 4.5 受邀者演示 `invited-person`

发起者先确认小纸条中可分享的称呼、头像、主题和留言。受邀者仅见这些摘要，下面填写自己的事实、感受、需要，完成安全选择和分享确认。

提供提交、存草稿、暂不参与；明确“提交后这三段话提供给发起者”“本机不验证身份”。等待、过期、撤回、拒绝、提交后和风险状态分别呈现。

受邀模式隐藏任务栏及普通返回，页尾“结束演示，回到发起者”用于角色切换。风险处理保留在受邀者区域，避免跳到包含发起者信息的界面。该隔离只保障演示路径，不是账号访问控制。

### 4.6 报告 `report`

顶部范围说明 + 状态卡 + 四个胶囊分区 Tab + 当前分区正文。分区为“共识事实 / 分歧说法 / 互动循环 / 修复行动”，对应 `consensus/difference/loop/repair`。

| 状态 | 展示 |
|---|---|
| 空 | “还没有聊过的事”，引导开始复盘 |
| 生成中 | 本地模板说明、36px loader，不给真实任务进度百分比 |
| 生成/结构失败 | 保留输入，危险提示卡，重试按钮 |
| 单方就绪 | 明确不能验证经过或替对方猜心 |
| 有对方补充 | “我的视角 · 含对方视角补充”，逐项标明本人填写/转述 |
| 邀请等待 | 48h 有效、撤回/单方查看/模拟对方填写等入口 |
| 拒绝/撤回/过期 | 保留单方结果，不施加催促压力 |

“共识事实”是当前标签原文，但正文实际为“单方可观察事实”；后续应改为按报告范围呈现适当标签，不能以标签把单方叙述确认成共识。正常流程没有真实共同报告。

修复区含本人愿意调整的内容、三条固定行动、选择与完成按钮、沟通草稿、两个反馈按钮和文本框。没有责任双栏百分比图、行动编辑表单或后续回顾页。

### 4.7 我的 `mine`

主标题“我的小角落”，按个人选择 → 伴侣与关怀 → 关系画像 → 历史摘要 → 授权管理 → 数据操作排序。历史当前只显示事件和线索两条演示摘要，无点击详情、筛选或趋势。

头像与欢迎页共用样式和状态。“撤回邀请”“标记过期”为事件邀请操作，不表示解除伴侣绑定。导出只是 Toast；说明文案中“导出文件”尚需与按钮实际能力统一。

### 4.8 伴侣与关怀 `companion`

单独的“我的”二级页面，结构为绑定区 → 可选身体记录 → 关怀状态与伴侣预览。

| 绑定状态 | 组件与操作 |
|---|---|
| `none` | 双方称呼输入、发起者成年人及意愿确认、邀请绑定 |
| `pending` | 双方称呼、模拟对方确认、确认绑定/暂不绑定/撤回 |
| `bound` | 双方称呼、模拟伴侣“愿意接收经期提醒”勾选、解除绑定 |

身体记录以“那几天，想被怎样照顾？”开头，适用性由用户决定。观察选择 `yes/unsure` 时展开最近开始日期、预计下次日期、提前天数、提醒话语、本机保存说明和日期共享勾选。`skip/no` 折叠；字段显示不代表已经保存。

预计日可空，辅助文案直接写明本人填写、不做医学预测。最近日期不可晚于今天；日期错误在表单内显示。未绑定时共享勾选禁用。

三个许可层级在文案和视觉上分别说明：

1. “我同意把这些敏感记录保存在当前浏览器”：说明未加密及共用设备风险。
2. “和绑定伴侣分享经期日期，并提前提醒对方”：说明共享最近日、预计日和话语，观察回答不共享。
3. 绑定卡内的接收提醒选择：说明关闭后日期仍可见，只有提醒停止。

关怀状态需区别未绑定、未共享、无预计日、日期过期、未开启接收、未到窗口、到达窗口。所有预览保留“本机演示、未发送”说明，不用成功通知图标暗示消息已送达。

预览只展示最近日、预计日和话语，不出现观察回答、激素推断或冲突记录。“模拟到期提醒（不发送）”是检查文案的演示入口，不代表今天已到期。

### 4.9 安全、退出与删除 `safety`

安全页使用浅中性底色和低饱和红色，标题“先照顾好你的安全”。提供安全退出、仅存本机、删除；不撒花、不用动物口吻淡化危险，不要求继续调解。

当前安全退出只回首页，不能表述为关闭网页、清除痕迹或联系了支持机构。

| 操作 | 当前确认方式 | 应清晰说明的范围 |
|---|---|---|
| 我的彻底删除 / 安全页删除 | 自定义遮罩弹窗，取消/确认；支持 Esc | 当前实现会影响事件、邀请、绑定和身体记录，且仍有残留，需修复后统一范围 |
| 删除身体记录与提醒 | 浏览器原生 confirm | 删除日期、观察和提醒设置；保留绑定，已被看过的信息无法收回 |
| 解除绑定 | 浏览器原生 confirm | 清空绑定及接收偏好、关闭共享，保留本人记录 |
| 改为跳过/无观察后保存 | 有既存日期时原生 confirm | 清除身体记录并关闭共享/提醒 |
| 关闭共享 | 直接执行 + Toast | 保留本人记录、停止日期展示与后续提醒 |

正式小程序应将原生浏览器确认转换为平台确认组件，并先明确“单条事件删除”“全部数据删除”的不同范围。

## 5. 文案原则与状态表达

使用“当前线索”“我当时的解释”“尚需确认”“由我转述”“可以尝试”。当前按钮“生成 AI 复盘”旁必须保留模板演示说明，不能让按钮名称掩盖能力边界。

| 场景 | 当前文案示例 / 应保留的信息 |
|---|---|
| 首页 | “今天，想从哪里说起？” |
| 探索结果 | “这是此刻的你，不是定义” |
| 对方补充 | “由我转述，尚需对方确认” |
| 本机隐私 | “共用设备上别人可能读到”，避免绝对私密承诺 |
| 关怀 | 本人观察、本人手填日期，不用它解释争执或否定感受 |
| 日期共享 | 明确可见字段；接收提醒是对方单独的偏好 |
| 关闭/删除 | 说明未来展示停止，已被对方看过的信息不能收回 |

禁止人格/疾病定论、性别刻板判断、胜败和责任百分比、医学预测、催促对方参与、宣称绑定就能自动查看所有数据。

## 6. 可访问性与动效

已具备：输入 label、选择器 `fieldset/legend`、头像替代文字、`aria-pressed`、任务键 `aria-current`、报告标签 `aria-selected`、错误 `role=alert`、Toast/关怀状态 live region、删除弹窗 dialog 语义。

动效：按钮背景/位移约 150–160ms，进度条 200ms，loader 760ms 循环，Toast 约 2200ms。`prefers-reduced-motion` 将过渡和动画降至近乎即时。

正式验收要求与当前差距：

1. 触控目标至少 44px；返回目前 34px，部分 `.choice/.chip/.checkline` 最小高度仅 38px，需核验实际点击区并补足。
2. 文字对比度需实测，不能仅根据 Token 宣称达标；状态不能只靠颜色区分。
3. 报告 Tab 应补齐键盘切换、面板关联和重绘后的选中态同步。
4. 删除弹窗需补焦点约束及关闭后的焦点归还；当前只在打开时聚焦“取消”。
5. 表单错误需关联具体字段并移动到可见位置；原型只有部分通用提示。
6. 验证 320/390/430px 宽度、长中文、系统字体放大、键盘弹起与底部安全区。1040px、520px 仅是现有断点，不是已通过测试的声明。

## 7. 后续设计交付与验收

当前 10 个页面 ID 与 PRD 对齐。设计验收应覆盖欢迎、首页、六题、六步复盘、四分区报告、受邀者、我的、安全和伴侣与关怀的主要状态。

本次同步不新增独立的量表评分、共同确认、云端邀请、行动回顾、趋势或通知收件箱设计。真实共同复盘、字段授权、导出和通知上线时，再基于 PRD/Tech 中的待实现要求补充页面及失败状态。

优先完善：删除范围与文案一致、单方报告标签、导出提示、到期与模拟预览区分、表单离开保存、报告 Tab 同步、44px 触控与移动端视觉验证。旧版粉色规范、8 月截图以及“V2”静态说明文字都不能替代最新 HTML 的实际状态。
